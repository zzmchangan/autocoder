# 新会话接力 prompt(复制下方「开始」到「结束」之间全部内容,粘贴进新会话)

<!-- 开始 -->

你将接手并完成《深入浅出系列》的《TiKV 设计与实现深入浅出:为什么能把一个 Raft 放大成百万个》一书。

**工作目录**:`c:\Users\86133\Desktop\深入浅出系列`
**当前进度**:总纲、写作提示词、目录与导读、P0-01 样板均已写好;`tikv`(`tikv/tikv` 的 Rust 实现)源码已 clone 到系列根目录(`../tikv/`,master @ `852b977`,版本 `9.0.0-beta.2`,nightly Rust)。
**你的目标**:用多 agent 并行,写完 22 章(P0-01 ~ P7-22)+ 附录 AB,范围是全书第 0~7 篇全部写完。

## 第一步:必读(重建上下文,按顺序)
1. `TiKV设计与实现/全书规划-总纲.md` —— 主线(百万个 Raft 组分片 + Percolator 跨组 ACID)、二分法(复制层 vs 事务层)、**三重承接**(Raft←etcd / RocksDB←LevelDB / RPC←gRPC)、分篇分章、**架构演进交代**(第五节)。
2. `TiKV设计与实现/_章节写作提示词.md` —— **这是你的执行手册**:写作铁律、四段式 + 技巧精解、源码规范、**三重承接坑 + 架构演进坑(raftstore v1/v2、RaftEngine)**、附 22 章清单与并行分组。
3. `TiKV设计与实现/目录与导读.md` —— 每章一句话钩子 + 技巧标签 + 二分法归属,选章 / 排查速查。
4. `TiKV设计与实现/P0-01-第一性原理-为什么需要TiKV.md` —— **风格样板**,所有 agent 一律对齐。
5. 已加载的 memory 方法论:`source-code-series-method`(动机 + 技巧双线)、`source-code-book-multi-agent`(多 agent 编排 + 核验三板斧);承接的三本可读:`etcd设计与实现/`(Raft)、`leveldb设计与实现/`(RocksDB/LSM)、`gRPC设计与实现/`(RPC)。

## 铁律(每一章都必须遵守)
- **动机(why) + 技巧(how) 双线**:每章单开"技巧精解"小节,挑最硬核 1~2 个技巧、配真实源码 + 反面对比拆透。**讲不清 FSM/batch-system、Percolator = 没讲 TiKV。**
- **源码严禁凭记忆**:基于 `../tikv/`(@ 852b977);每处引用先 Grep / Read 核实行号,不确定就**只标文件不标行号**。简化代码必须标注"(简化示意)"。
- **三重承接,不重复造轮子**:Raft 算法 → 指《etcd》、RocksDB/LSM → 指《LevelDB》、gRPC/RPC → 指《gRPC》。涉及这三块时一句话带过 + 指路,把篇幅留给 TiKV 独有的(multi-raft、Percolator、scheduler/latch、RaftEngine)。
- **⚠️ 架构演进**:① 经典 raftstore(v1)为主线、raftstore-v2 作对照;② **Raft 日志用新版 RaftEngine**(`components/raft_log_engine`),不是老资料的"存 RocksDB raft CF";③ in_memory_engine/resource_control 为 8.x/9.x 新增。每章产出标注"经典 v1 / v2 / 对照"。老博客大片过时,不能当唯一依据。
- **标点**:半角 `,` `:` `()` `?` + 全角 `、` `——` `""` `。`(句号全角),中英文之间留空格。
- **配图**:时序 / 状态 / 流程用 `mermaid`;内存 / 数据结构布局(MVCC key+ts 编码、三 CF、Region 元数据)用 ASCII 框图。
- **比喻**:直球为主、比喻点睛。快递员比喻只在 P0-01 点睛,其他章不得沿用做主线。
- **命名**:`TiKV设计与实现/P{篇}-{章}-标题.md`;每章约 8000~12000 字。

## 执行方式:用 Agent 工具多 agent 并行(按提示词附录的分组)
- **第一波(顺序,1 个 agent)**:第 1 篇 P1-02 → P1-03 → P1-04(multi-Raft 原理递进)+ 第 2 篇 P2-05 → P2-06 → P2-07 → P2-08(raftstore 层层递进)。地基,同一 agent 顺序写,保证术语 / 行号 / 比喻一致。
- **第二波(四篇并行,4 个 agent)**:第 3 篇(P3-09~11)/ 第 4 篇(P4-12~15,Percolator 核心)/ 第 5 篇(P5-16~18)/ 第 6 篇(P6-19~21)各开一个 agent;篇内按章序推进。注意 P4 依赖 P3-10(MVCC 编码),先写 P3-10 或由同一 agent 串。
- **第三波(收尾)**:P7-22 全书收束 + 附录 A 源码路线图 + 附录 B 工具链,最后写。
- 每个 agent 的任务模板:读"提示词 + 总纲 + 本篇相邻章 + P0-01 样板" → Grep / Read 核源码 → 写指定章节 → 过自检清单 → 产出 `P{篇}-{章}-标题.md`。
- **并行要点**:同一批要并行的 agent,放在**一条消息里多个 Agent 调用**,才会并发跑。

## 核验三板斧(主控每波收稿时做)
1. **Grep 扫结构**:每章是否有章首核心问题 / 正文四段式 / 技巧精解 / 章末五问。
2. **抽源码行号**:每章抽查 2~3 处源码引用,Read `../tikv/` 对应文件,看行号 / 函数名逐字吻合。
3. **看 agent 是否主动修正印象**:好的 agent 会 Grep 后发现"这里 Raft 日志用 RaftEngine 不是 RocksDB""这是 raftstore v2 不是 v1",在稿子里写出来——这是质量信号。同时检查承接处是否"指路不重复"(没把 Raft/RocksDB/gRPC 重讲一遍)。

## 自主推进授权
你被充分授权**自主推进**:读文件 → 启动 agent → 核验产出(抽查源码行号真实、技巧讲透、三重承接到位、架构演进分清、标点合规) → 继续下一波,直到 22 章 + 附录全部完成。每完成一波向我简短汇报(哪些章完成、各章字数、引用的关键源码文件、承接/架构演进处理)。只在遇到"风格 / 范围 / 深度 / raftstore v1 还是 v2 主线"这类需要拍板的事时才问我,写作细节自己定。

**现在开始**:先读上面四个文件,然后启动第一波 agent(第 1 篇 + 第 2 篇地基)。

<!-- 结束 -->
