# 新会话接力 prompt(复制下方「开始」到「结束」之间全部内容,粘贴进新会话)

<!-- 开始 -->

你将接手并完成《深入浅出系列》的《gRPC 设计与实现深入浅出:为什么把方法调用变成一条流》一书。

**工作目录**:`c:\Users\86133\Desktop\深入浅出系列`
**当前进度**:总纲、写作提示词、目录与导读、P0-01 样板均已写好;`grpc`(`grpc/grpc` 的 C++ core)源码已 clone 到系列根目录(`../grpc/`,master @ `2195e869`,版本 `1.83.0-dev`)。
**你的目标**:用多 agent 并行,写完 23 章(P0-01 ~ P7-23)+ 附录 AB,范围是全书第 0~7 篇全部写完。

## 第一步:必读(重建上下文,按顺序)
1. `gRPC设计与实现/全书规划-总纲.md` —— 主线(协议层 vs 框架层)、二分法、**直球为主比喻点睛**基调、分篇分章(每章核心问题/源码/技巧)、**架构演进交代**(第五节,gRPC 正从 callback+CQ 迁移到 Promise+call spine)。
2. `gRPC设计与实现/_章节写作提示词.md` —— **这是你的执行手册**:写作铁律、四段式 + 技巧精解、源码规范、配图、标点、**gRPC 独有大坑(架构演进)、附 23 章清单与并行分组**。
3. `gRPC设计与实现/目录与导读.md` —— 每章一句话钩子 + 技巧标签 + 二分法归属,选章 / 排查时速查。
4. `gRPC设计与实现/P0-01-第一性原理-为什么需要gRPC.md` —— **风格样板**(若已写),所有 agent 一律对齐它的文风 / 结构 / 源码引用。
5. 已加载的 memory 方法论:`source-code-series-method`(动机 + 技巧双线)、`source-code-book-multi-agent`(多 agent 编排 + 核验三板斧)。

## 铁律(每一章都必须遵守)
- **动机(why) + 技巧(how) 双线**:每章单开"技巧精解"小节,挑最硬核 1~2 个技巧、配真实源码 + 反面对比("不这么写会怎样")拆透。**讲不清 HPACK / flow control / filter fusion = 没讲 gRPC。**
- **源码严禁凭记忆**:基于 `../grpc/`(C++ core);每处引用先 Grep / Read 核实行号,不确定就**只标文件不标行号**。简化代码必须标注"(简化示意)"。
- **⚠️ 架构演进**:gRPC core 正从经典(callback + completion queue + filter stack)迁移到新(Promise + call spine + filter fusion)。**以新版源码结构为准**(`src/core/` 已扁平化,无老 `lib/` 层),经典架构作背景对照,legacy 文件(`*_legacy`)诚实标注。老博客大片过时,不能当唯一依据。每章产出时标注"经典形态 / 新 Promise 形态 / 两者对照"。
- **HTTP/2 对得上 RFC**:HPACK 对 RFC 7541、帧 / 流控对 RFC 7540。讲错 RFC 是硬伤。
- **标点**:半角 `,` `:` `()` `?` + 全角 `、` `——` `""` `。`(句号全角),中英文之间留空格。
- **配图**:时序 / 状态 / 流程用 `mermaid`;内存 / 数据结构布局(HPACK 动态表、flow control 双层 window、slice 引用计数、arena)用 ASCII 框图(**不是**几何 ASCII)。
- **比喻**:直球为主、比喻点睛。电话比喻只在 P0-01 点睛,其他章不得沿用做主线。
- **命名**:`gRPC设计与实现/P{篇}-{章}-标题.md`;每章约 8000~12000 字。

## 执行方式:用 Agent 工具多 agent 并行(按提示词附录的分组)
- **第一波(顺序,1 个 agent)**:第 1 篇 P1-02 → P1-03 → P1-04(契约三章有依赖,顺序写)+ 第 2 篇 P2-05 → P2-06 → P2-07 → P2-08 → P2-09(HTTP/2 五章层层递进,顺序写)。这两篇是全书地基,建议由同一 agent 顺序完成,保证术语 / 行号 / 比喻一致。
- **第二波(四篇并行,4 个 agent 同时启动)**:第 3 篇(P3-10~12)/ 第 4 篇(P4-13~16)/ 第 5 篇(P5-17~19)/ 第 6 篇(P6-20~22)各开一个 agent;篇内按章序推进。
- **第三波(收尾)**:P7-23 全书收束 + 附录 A 源码路线图 + 附录 B 工具链,最后写。
- 每个 agent 的任务模板:读"提示词 + 总纲 + 本篇相邻章 + P0-01 样板" → Grep / Read 核源码 → 写指定章节 → 过自检清单 → 产出 `P{篇}-{章}-标题.md`。
- **并行要点**:同一批要并行的 agent,放在**一条消息里多个 Agent 调用**,才会并发跑。

## 核验三板斧(主控每波收稿时做)
1. **Grep 扫结构**:每章是否有章首核心问题 / 正文四段式 / 技巧精解 / 章末五问。
2. **抽源码行号**:每章抽查 2~3 处源码引用,Read `../grpc/` 对应文件,看行号 / 函数名逐字吻合(凭记忆编行号是最常见硬伤)。
3. **看 agent 是否主动修正印象**:好的 agent 会 Grep 后发现"咦,这个函数 6.x 改名了 / 这段是新 Promise 形态不是经典 callback",在稿子里写出来——这是质量信号。

## 自主推进授权
你被充分授权**自主推进**:读文件 → 启动 agent → 核验产出(抽查源码行号是否真实、技巧是否讲透、HTTP/2 是否对得上 RFC、标点是否合规、架构演进是否分清) → 继续下一波,直到 23 章 + 附录全部完成。每完成一波向我简短汇报(哪些章完成、各章字数、引用的关键源码文件、经典 / Promise 形态分布)。只在遇到"风格 / 范围 / 深度 / 架构演进取舍"这类需要拍板的事时才问我,写作细节自己定。

**现在开始**:先读上面四个文件,然后启动第一波 agent(第 1 篇 + 第 2 篇地基)。

<!-- 结束 -->
