export const meta = {
  name: 'linux-kernel-mechanism-full-book',
  description: '一口气写完《Linux内核机制深入浅出》正文19章+P5-21+附录AB(共22单元)',
  phases: [
    { title: '正文', detail: 'P1-02~P4-20 共19章,篇间并行' },
    { title: '收尾', detail: 'P5-21 哲学+对照总表 / 附录A 全景脉络 / 附录B 源码路线' },
  ],
}

// ===================== 共享写作规范(所有 agent 必读) =====================
const COMMON = [
  '你是资深 Linux 内核工程师(深谙 kernel/irq/、kernel/softirq.c、kernel/workqueue.c、kernel/entry/、kernel/time/、kernel/signal.c、x86 体系结构、SMP、中断/时钟/信号子系统)兼技术作者,正在合著《Linux 内核机制深入浅出:中断·系统调用·时钟·信号》。本书是《深入浅出系列》第 12 本(与 Tokio/LevelDB/etcd/Go runtime/内存分配器/Linux mm/Linux 调度器 同源)。面向"写过 Linux 用户态程序、读过操作系统课、甚至翻过相关源码,却一知半解"的读者。你的任务是写出高质量章节,让"读过源码没看懂"的人真的能讲清一次中断/一次系统调用/一个 hrtimer/一个信号的旅程,以及每一步底下的实现技巧。',
  '',
  '【动手前必读这 5 个文件(用 Read 工具),获取完整规范与风格】',
  '1. linux-内核机制设计与实现/全书规划-总纲.md(主线/二分法/分篇分章/源码策略/内核 C 技巧侧重/★对照特色)',
  '2. linux-内核机制设计与实现/_章节写作提示词.md(铁律/四段式/技巧精解/自检清单/风格禁区/已知坑)',
  '3. linux-内核机制设计与实现/P0-01-第一性原理-为什么内核要管中断与系统调用.md(定调样本,严格对齐其风格、结构、标点)',
  '4. linux-内核机制设计与实现/目录与导读.md(本章在全书位置 + 相邻章标题)',
  '5. 可选风格参考:linux-mm设计与实现/ 与 linux-sched设计与实现/ 下任一已完成章(质量标杆)。时钟篇必须读 linux-sched/ 下 hrtick 相关内容以便回扣。',
  '',
  '【全书第一性原理——贯穿始终,反复回扣】',
  '中断/异常把控制权从用户态拉进内核、系统调用是用户态合法进内核的入口、时钟驱动调度与定时、信号是内核向进程的异步通知——这四个机制都是"事件跨越用户/内核边界",合起来构成内核的事件驱动骨架。',
  '二分法(迷路时回到它):进内核(中断/异常/系统调用入口,事件把控制权拉进内核) vs 内核主动驱动/通知(时钟触发调度、信号投递,内核向外);另有"支撑"(IRQ 抽象、时钟源、pending 队列、preempt_count 等地基)。',
  '',
  '【命脉:动机(why) + 技巧(how) 双线 ⚠️】',
  '读者"一知半解"的缺口不在动机(好懂),在技巧(看不懂)。每个关键代码点必须讲两件事:1. 设计动机(解决什么本质问题/不这样会怎样);2. 实现技巧(用了什么手段/为什么妙/不这么写会撞什么墙)。每章单开"技巧精解"小节(正文后、小结前),挑最硬核 1~2 个技巧 + 真实源码片段 + 行号 + 反面对比。讲不清技巧 = 没讲 Linux 内核机制。',
  '',
  '【基调:直球讲透为主,比喻只在反直觉处点睛】(延续 LevelDB/mm/调度器)',
  '默认用表/流程图/源码直球讲清;仅当反直觉(中断上下文不能睡眠、信号延迟到返回用户态才处理、NOHZ 停 tick 又不丢、SYSCALL 凭什么比 int 0x80 快)时,用一两句比喻点睛(softirq 可点"中断的续集";信号可点"挂号信,内核先签收、等你回家才当面交给你")。判断标准:直球讲不清才上比喻。',
  '',
  '【★ 三向对照(本书独有特色)】',
  '若本章标 ★,在技巧精解后加一个轻量"★对照"小栏(3~5 行):中断上下半部 ↔ Tokio mio epoll 就绪 + 异步 task 接力;hrtimer 红黑树 ↔ Tokio 时间轮(批量 O(1),精确 vs 批量的取舍);NOHZ idle 停 tick ↔ Tokio 进程空闲 park 线程;信号延迟投递 ↔ Go channel(异步延迟)/panic(同步杀死);SYSCALL 指令入口 ↔ Go runtime systrap;传统中断 ↔ io_uring CQE(内核主动通知 vs 用户主动轮询,事件模型代际差)。点到为止。',
  '',
  '【每章标准结构(顺序不可变)】',
  '1. 章首:核心问题(一句话)+ "读完本章你会明白……"(3~4 条)。难度高的加逃生阀。',
  '2. 章首一句话点破:结论/金句,然后"这是结论不是理由,本章倒过来拆"。',
  '3. 正文若干小节:招牌四段式"提出问题 → 不这样会怎样 → 所以这样设计 → 源码/技巧佐证";用 > 引用块强调: > **不这样会怎样**:…  /  > **所以这样设计**:…  /  > **钉死这件事**:…',
  '4. 技巧精解(正文后、小结前):1~2 个最硬核技巧 + 真实源码 + 行号 + 反面对比。',
  '5. ★对照(仅标 ★ 章,技巧精解后):3~5 行。',
  '6. 章末小结:回扣二分法(本章服务"进内核/内核主动/支撑"哪面)+ 五个"为什么"清单 + "想继续深入往哪钻"(源码文件、/proc/perf/ftrace/strace/bpftrace 观测、延伸)+ 引出下一章。',
  '',
  '【配图策略】时序/状态/流程用 mermaid(sequenceDiagram/stateDiagram/flowchart);数据结构布局(irq_desc/irq_data/irq_chip 关系、hrtimer_cpu_base/红黑树嵌套、signal_struct/sighand_struct/sigpending、preempt_count bit 位)用 ASCII 框图或表格。铁律:不要用 ASCII 画时序。',
  '',
  '【源码策略——严禁凭记忆,每处引用动手前 Grep/Read 核实】',
  '- 工作目录是项目根"深入浅出系列";本地源码根 linux/(Linux 6.9 sparse)。写入路径:linux-内核机制设计与实现/{本章文件名}。',
  '- markdown 源码链接用相对路径 ../linux/kernel/...(从书目录看 linux 是兄弟目录,这样 VSCode 预览可点击):行内 [描述](../linux/kernel/xxx.c#L起-L止),单行 #L,区间 #La-Lb;头文件 ../linux/include/linux/xxx.h。',
  '- arch/x86 未 sparse clone:entry_64.S / syscall_64.c / arch/x86/kernel/signal.c(__setup_rt_frame)/ VDSO 等,只描述作用 + 引通用框架 kernel/entry/common.c,不标 arch 具体行号。',
  '- 宏(SYSCALL_DEFINE* / local_irq_save / preempt_disable / IDT / asm 内联)必须展开或解释,绝不贴一段宏不解释。',
  '- 已确认的 6.9 真实行号锚点(仍以你 Grep 核实为准,这些是主控抽查过的):',
  '  softirq.c: handle_softirqs@511(6.9 主体逻辑在此,含 pending/ffs/MAX_SOFTIRQ_RESTART/wakeup_softirqd)、__do_softirq@586(已瘦身为调 handle_softirqs(false) 的薄包装)、irq_enter_rcu@594、__irq_exit_rcu@627、irq_exit@659、softirq_vec[]@60、open_softirq@703、raise_softirq@687、raise_softirq_irqoff@670、invoke_softirq@277 与 @419。',
  '  hrtimer.c: __hrtimer_next_event_base@505、__hrtimer_run_queues@1724、hrtimer_interrupt@1788、hrtimer_run_queues@1901。',
  '  tick-sched.c: tick_sched_do_timer@206、tick_nohz_restart_sched_tick@1086、tick_nohz_idle_enter@1250、tick_nohz_irq_exit@1287。',
  '  signal.c: complete_signal@995、__send_signal_locked@1074、do_send_sig_info@1294、get_signal@2675。',
  '  entry/common.c(函数带 noinstr/__visible 修饰前缀):syscall_enter_from_user_mode_prepare@74、arch_do_signal_or_restart@83、exit_to_user_mode_loop@90、syscall_exit_to_user_mode@215、irqentry_enter_from_user_mode@223、irqentry_exit_to_user_mode@228、irqentry_enter@236、irqentry_exit@328。',
  '  workqueue.c: __queue_work@2313、process_one_work@3166、worker_thread@3374。',
  '  irq/handle.c: __handle_irq_event_percpu@139、handle_irq_event@202。include/linux/irq.h: struct irq_data@179、struct irq_chip@501。include/linux/hrtimer_types.h: struct hrtimer@39。include/linux/timekeeper_internal.h: struct timekeeper@92、struct tk_read_base@34。include/linux/clocksource.h: struct clocksource@96。include/linux/sched/signal.h: struct sighand_struct@21、struct signal_struct@94。',
  '- 中断上下文/抢占/锁/内存序技巧必须讲清"为什么 sound":不会在 hardirq 里睡眠、不会丢唤醒、信号 pending 不会被并发损坏、hrtimer 重编程不会丢中断;必要时引用源码注释。',
  '- 简化展示的代码标注"(简化示意,非源码原文)";不确定精确行号时,宁可只标文件不标行号。',
  '',
  '【已知坑(务必避开,这些都是 6.x 演进点,老资料会翻车)】',
  '- 讲 softirq 主循环必须引 handle_softirqs@511,不要照搬讲 __do_softirq 含主循环的老资料(6.9 __do_softirq 已瘦身)。',
  '- 讲定时器必须基于 hrtimer 红黑树(timerqueue),老的低精度 timer wheel 轮早已废弃为 hrtimer 的低分辨率模式,别照搬老资料。',
  '- entry/common.c 函数名带 noinstr/__visible 修饰,Grep 时不要要求行首 ^,否则会漏。',
  '',
  '【标点硬性要求】半角 ASCII 逗号 ,、冒号 :、括号 ()、问号 ? + 全角顿号 、破折号 —— 引号 "" 句号 。;中英文之间留空格;句号必须全角 。。',
  '',
  '【篇幅】每章约 8000~12000 字(P0-01 样本约 9000 字)。源码精读 + 技巧精解,对标 mm/调度器深度。',
  '',
  '【风格禁区】不写成"request_irq/signal 怎么调"的运维书;不只讲 why 不讲 how 技巧;不贴宏/体系结构代码不解释;不讲中断上下文/锁技巧不讲为什么 sound;不滥用比喻;不编造源码行号;不与前文重复;不照搬讲 __do_softirq 主循环或 timer wheel 的老资料;通读不得出现"显然/众所周知"。',
  '',
  '【交付前自检清单】每小节先 why 后 how?每个关键点讲了动机+技巧两面?技巧精解有真实源码+反面对比?宏/体系结构代码解释了?标 ★ 章有对照栏?所有源码行号 Grep 核过一致?中断/锁技巧讲了为什么 sound?softirq 引 handle_softirqs 而非过时 __do_softirq 内联?时序用 mermaid、结构用 ASCII,没用 ASCII 画时序?标点半角逗号冒号括号问号 + 全角句号,中英文间空格?章末有五问 + 往哪钻 + 引出下章 + 回扣二分法?通读无"显然/众所周知"、无与前文重复?',
  '',
  '【交付】写完文件后,调用 StructuredOutput 工具按 schema 返回报告(章节、路径、字数、关键源码文件、写作中 Grep 核实后修正的源码印象、结构完整性、备注)。',
  '',
  '======================= 本章任务 =======================',
  '',
].join('\n')

// ===================== 19 章正文 =====================
const CHAPTERS = [
  {
    id: 'P1-02',
    file: 'P1-02-硬件中断与中断向量-CPU怎么被拉进内核.md',
    task: [
      '【第 1 篇·第 2 章·硬件中断与中断向量:CPU 怎么被拉进内核】',
      '- 文件名:P1-02-硬件中断与中断向量-CPU怎么被拉进内核.md',
      '- 核心问题:CPU 是怎么"被动"被拉进内核的——每条指令结束后硬件查中断引脚、x86 中断向量号(0-255 区间,32 以上可屏蔽外部中断)、IDT(中断描述符表,每个向量一项门描述符)、IRQ 号 vs CPU 向量号的区别、handle_arch_irq 统一入口。',
      '- 二分法归属:进内核',
      '- 关键源码:linux/kernel/irq/handle.c(set_handle_irq 注册全局 handle_arch_irq 函数指针、__handle_irq_event_percpu@139、handle_irq_event@202);arch/x86 IDT/IDT 装载描述(未 sparse clone,描述 IDT 表结构 + 门描述符作用 + 引 handle.c 的 set_handle_irq,不标 arch 行号)。',
      '- 本章典型技巧:IDT 表 + CPU 自动保存上下文(CPU 硬件在跳转前自动压关键寄存器、查 IDT 拿到入口地址,是硬件做的工作)。',
      '- 反面对比:若用软件轮询网卡状态,会持续占 CPU、延迟高、多核无法并行响应;中断是"被动响应"的唯一高效方式。',
      '- ★对照:否',
      '- 前置衔接:从 P0-01"用户/内核边界是 OS 命脉 + 事件驱动 vs 轮询"接下来,正面讲"中断这一侧怎么把 CPU 从用户态拉进内核"。',
      '- 特殊要求:讲清"中断向量号(CPU 看的 0-255)"与"Linux IRQ 号(软件抽象的编号)"不是一回事,为下一章 P1-03 irq_domain 的 hwirq→IRQ 映射铺垫;IDT 是体系结构相关,arch/x86 未 sparse clone,描述其作用为主。',
    ].join('\n'),
  },
  {
    id: 'P1-03',
    file: 'P1-03-IRQ-domain与中断控制器抽象-irq_chip-irq_domain.md',
    task: [
      '【第 1 篇·第 3 章·IRQ domain 与中断控制器抽象:irq_chip/irq_domain】',
      '- 文件名:P1-03-IRQ-domain与中断控制器抽象-irq_chip-irq_domain.md',
      '- 核心问题:硬件中断控制器五花八门(x86 LAPIC/IOAPIC、ARM GIC、MSI/MSI-X),Linux 怎么抽象成统一接口——struct irq_chip(对硬件的操作:ack/mask/unmask/eoi/set_affinity 等)、struct irq_domain(硬件中断号 hwirq → Linux IRQ 号的映射,支持 linear/radix-tree/hierarchical 三种)、irq_desc(每个 IRQ 的描述符,挂 action 链表与 irq_data)。',
      '- 二分法归属:支撑',
      '- 关键源码:linux/kernel/irq/irqdomain.c(irq_domain_create_linear/add_simple、__irq_domain_alloc_irqs)、linux/kernel/irq/chip.c(irq_chip 操作集、handle_edge_irq/handle_level_irq 流控)、linux/kernel/irq/manage.c(request_threaded_irq@2147);linux/include/linux/irq.h(struct irq_data@179、struct irq_chip@501、struct irq_common_data@147);linux/kernel/irq/internals.h(irq_desc 内部)。',
      '- 本章典型技巧:irq_domain 的层级映射(hierarchical domain,支持 GIC 父级 + MSI 子级的级联, hwirq 在各级翻译最终落到 Linux IRQ)。',
      '- 反面对比:若每个驱动自己维护"硬件中断号→处理函数"映射表,会全局混乱、无法支持中断控制器级联与 MSI。',
      '- ★对照:否',
      '- 前置衔接:从 P1-02"CPU 看的是向量号"接,讲 Linux 怎么在向量号和驱动看到的 IRQ 号之间架一层 irq_domain。',
      '- 特殊要求:用 ASCII 框图画清 irq_domain → irq_desc → irq_data → irq_chip 的关系;讲 hierarchical domain 时举 GIC + MSI 级联例子。',
    ].join('\n'),
  },
  {
    id: 'P1-04',
    file: 'P1-04-中断上下文-为什么不能睡眠.md',
    task: [
      '【第 1 篇·第 4 章·中断上下文:为什么不能睡眠】',
      '- 文件名:P1-04-中断上下文-为什么不能睡眠.md',
      '- 核心问题:中断上下文(hardirq context)的本质——它不是进程,没有可调度的 task_struct,current 指向的是被中断的进程但中断不属于它;preempt_count 的 HARDIRQ_OFFSET/SOFTIRQ_OFFSET/NMI_OFFSET 计数、in_interrupt()/in_softirq()/in_irq() 判定;为什么在中断里不能 sleep、不能 mutex(没有 task 可挂起、调度器无物可切)。',
      '- 二分法归属:支撑',
      '- 关键源讲:linux/kernel/softirq.c(irq_enter_rcu@594 给 preempt_count 加 HARDIRQ_OFFSET、__irq_exit_rcu@627 递减并触发软中断);linux/include/linux/preempt.h(preempt_count 的 bit 位布局:bit0-7 抢占计数、bit8-15 软中断、bit16-19 硬中断、NMI;in_interrupt 等宏);linux/include/linux/hardirq.h。',
      '- 本章典型技巧:preempt_count 嵌套计数(用一段 bit 位同时表示 hardirq/softirq/nmi/抢占屏蔽,支持中断嵌套与"是否在中断里"的判断)。',
      '- 反面对比:若用一个布尔标志"是否在中中断",无法处理嵌套中断(NMI 打断 hardirq 打断 softirq);也无法区分"在哪一层"。',
      '- ★对照:否',
      '- 前置衔接:从 P1-03 接,讲"进了内核之后,中断运行在什么上下文、受什么约束"——这是 P1-05/06/07(下半部)为什么必须存在的根因。',
      '- 特殊要求:用 ASCII 表画 preempt_count 的 bit 位布局;务必讲清"为什么不能 sleep"(没有 task_struct 可挂起,mutex/sleep 依赖可调度实体);为下一章上半部/下半部切分埋下"中断里不能干重活"的伏笔。',
    ].join('\n'),
  },
  {
    id: 'P1-05',
    file: 'P1-05-上半部与下半部-为什么中断要切两段.md',
    task: [
      '【第 1 篇·第 5 章·上半部与下半部:为什么中断要切两段】',
      '- 文件名:P1-05-上半部与下半部-为什么中断要切两段.md',
      '- 核心问题:为什么中断处理要切两段——上半部(hardirq)快收快放(只做最紧急的:应答硬件、拷贝数据、标记),下半部(deferred work)延后做重活(协议栈处理、IO 后续);为什么不能在中断里干重活(占用 CPU、可能屏蔽其他中断、不可睡眠见 P1-04);三种下半部对比:softirq(轻量、per-CPU、不可睡眠)、tasklet(softirq 的包装,BH 时代遗物,6.x 在逐步废弃,讲清废除原因即可不单列)、workqueue(有进程上下文、可睡眠)。',
      '- 二分法归属:进内核(下半部机制仍服务"进内核"那一面的事件处理)',
      '- 关键源码:linux/include/linux/interrupt.h(request_irq/request_threaded_irq 声明、irq_handler_t@103、下半部注册接口);linux/kernel/softirq.c(softirq 入口概览);linux/kernel/workqueue.c(workqueue 入口概览)。',
      '- 本章典型技巧:上半部/下半部切分(性能与功能的切分——上半部抢占约束严要快,下半部可放开约束做重活)。',
      '- 反面对比:若全部在 hardirq 做(协议栈、磁盘后续),会长时间屏蔽其他中断、卡死系统、且无法调阻塞 API。',
      '- ★对照:是(对照 Tokio:mio 只读 epoll 就绪事件 = 上半部,异步 task 接力做后续 = 下半部,同构)',
      '- 前置衔接:从 P1-04"中断上下文不能睡眠、约束严"接,推出"所以要把可推迟的工作挪出去"。',
      '- 特殊要求:给出三种下半部(softirq/tasklet/workqueue)的对比表(能否睡眠、并发度、典型用途);点明 tasklet 即将废弃(6.x 计划移除,因 per-CPU softirq 包装带来不必要的复杂与竞争);为后两章 P1-06 softirq、P1-07 workqueue 铺路。',
    ].join('\n'),
  },
  {
    id: 'P1-06',
    file: 'P1-06-softirq软中断-per-CPU的延迟工作.md',
    task: [
      '【第 1 篇·第 6 章·softirq 软中断:per-CPU 的延迟工作】',
      '- 文件名:P1-06-softirq软中断-per-CPU的延迟工作.md',
      '- 核心问题:softirq 是中断的"续集"——hardirq 退出时若 pending 位图有位,irq_exit 路径 invoke_softirq → __do_softirq → handle_softirqs 在中断退出点(仍关部分抢占)就地处理;32 位的 per-CPU pending 位图、softirq_vec[] 数组(10 种软中断 NET_RX/NET_TX/TIMER/TASKLET 等)、open_softirq 注册、raise_softirq 标记、MAX_SOFTIRQ_RESTART/MAX_SOFTIRQ_TIME 防饿死、ksoftirqd 兜底线程。',
      '- 二分法归属:进内核(下半部)',
      '- 关键源码:linux/kernel/softirq.c(softirq_vec[]@60、raise_softirq@687、raise_softirq_irqoff@670、open_softirq@703、invoke_softirq@277 与 @419、__do_softirq@586 注意 6.9 已瘦身为调 handle_softirqs(false)、handle_softirqs@511 主体逻辑 pending=local_softirq_pending()/ffs 找位/MAX_SOFTIRQ_RESTART 循环/wakeup_softirqd、irq_enter_rcu@594、__irq_exit_rcu@627、irq_exit@659);linux/include/linux/interrupt.h(irq_cpustat_t、__softirq_pending、open_softirq 声明@608)。',
      '- 本章典型技巧:per-CPU pending 位图 + ffs 找第一个置位(per-CPU 无锁、O(1) 查待处理);MAX_SOFTIRQ_RESTART/MAX_SOFTIRQ_TIME 防饿死(softirq 循环限次,超了唤醒 ksoftirqd 内核线程接管,防 softirq 自激——网络 RX 触发 TX 触发 RX——永远占着 CPU)。',
      '- 反面对比:若无限制循环,softirq 自激会饿死普通进程;若无 per-CPU 位图用全局锁,多核收包会互相阻塞。',
      '- ★对照:是(对照 Tokio:mio 就绪事件 → 异步 task 调度接力;softirq 的 per-CPU 无锁对照 Tokio 的 per-worker 队列)',
      '- 前置衔接:从 P1-05"下半部延后做重活"接,讲第一种、也是最底层的下半部 softirq。',
      '- 特殊要求:⚠️ 6.9 讲 softirq 主循环必须引 handle_softirqs@511,不要照搬讲 __do_softirq 含主循环的老资料(__do_softirq@586 已瘦身);用 mermaid 流程图画 irq_exit → invoke_softirq → __do_softirq → handle_softirqs → softirq_vec[i]->action() 的完整路径;讲清 softirq 与硬中断退出点的关系(为何要在 irq_exit 就地处理而非另起线程)。',
    ].join('\n'),
  },
  {
    id: 'P1-07',
    file: 'P1-07-workqueue-可睡眠的延迟工作.md',
    task: [
      '【第 1 篇·第 7 章·workqueue:可睡眠的延迟工作】',
      '- 核心问题:softirq/tasklet 不能睡眠,但有些延后工作要调阻塞 API(mutex、磁盘 IO、分配可能回收的内存);workqueue 提供有进程上下文的延迟工作——work_struct + worker_pool + worker 内核线程、queue_work 入队、process_one_work 执行;CMWQ(Concurrency Managed Workqueue)按需唤醒 worker、并发度管制,既不爆炸又能并发跑阻塞工作。',
      '- 文件名:P1-07-workqueue-可睡眠的延迟工作.md',
      '- 二分法归属:进内核(下半部)',
      '- 关键源码:linux/kernel/workqueue.c(__queue_work@2313 选 pool/worker、queue_work_on@2439、process_one_work@3166 执行单个 work、worker_thread@3374 worker 主循环);linux/include/linux/workqueue.h(work_struct、INIT_WORK、queue_work 宏);linux/kernel/workqueue_internal.h(worker 结构)。',
      '- 本章典型技巧:CMWQ 的 worker 池管理(按需唤醒:有 work 才唤醒空闲 worker,繁忙时动态创建;并发度管制:同 bound pool 的多个 worker 并发跑不同 work,但管理线程数避免爆炸;用 manage_workers 平衡)。',
      '- 反面对比:若每 work 一个内核线程(老 workqueue 的做法),大量 work 会导致线程爆炸;若单线程串行(早期),无法并发跑阻塞 work。',
      '- ★对照:否',
      '- 前置衔接:从 P1-06 softirq"不可睡眠"接,讲"需要睡眠的下半部交给 workqueue"。',
      '- 特殊要求:对比 softirq(不可睡眠、per-CPU、轻量)与 workqueue(可睡眠、worker 池、较重);讲清 CMWQ 为何取代老 workqueue(并发管制);为第 2 篇系统调用收尾(中断篇到此结束)。',
    ].join('\n'),
  },
  {
    id: 'P2-08',
    file: 'P2-08-系统调用入口-SYSCALL指令与sys_call_table.md',
    task: [
      '【第 2 篇·第 8 章·系统调用入口:SYSCALL 指令与 sys_call_table】',
      '- 核心问题:用户态怎么"合法"进内核——x86_64 的 SYSCALL 指令 vs 老 int 0x80(软中断):SYSCALL 用 CPU MSR 寄存器直接跳到内核入口、不查 IDT、不压完整 trap frame,快一个数量级;do_syscall_64 入口;sys_call_table[] 函数指针数组(用系统调用号索引);六寄存器传参(rdi/rsi/rdx/r10/r8/r9);返回值放 rax。',
      '- 文件名:P2-08-系统调用入口-SYSCALL指令与sys_call_table.md',
      '- 二分法归属:进内核',
      '- 关键源码:arch/x86/entry/entry_64.S(SYSCALL 入口,未 sparse clone,描述其用 MSR_LSTAR 设入口、不压完整 frame)+ arch/x86/entry/syscall_64.c(sys_call_table,未 clone,描述其是函数指针数组);linux/kernel/entry/common.c(syscall_enter_from_user_mode_prepare@74 通用入口框架、syscall_exit_to_user_mode@215)。',
      '- 本章典型技巧:SYSCALL 指令的快路径(CPU 用 MSR_LSTAR 直接跳内核入口、SYSCALL 不切换栈硬件压完整 frame、SYSRET 对称返回;vs int 0x80 走 IDT 查询 + 完整 trap frame)。',
      '- 反面对比:int 0x80 走 IDT 查询 + 压完整 trap frame + 中断门开销,慢一个数量级;SYSCALL 把这些硬件开销省掉。',
      '- ★对照:是(对照 Go runtime:runtime.systemstack_switch/asmsysdyn 的 systrap,都是"用户态合法进内核/进 runtime"的受控通道)',
      '- 前置衔接:从第 1 篇中断"事件被动把 CPU 拉进内核"接,讲"用户主动、合法进内核"的另一面——系统调用。',
      '- 特殊要求:arch/x86 未 sparse clone,SYSCALL 入口只描述作用 + 引通用框架 kernel/entry/common.c;讲清 SYSCALL 与 int 0x80 的硬件级差异(MSR vs IDT、frame 压栈量);sys_call_table 是用调用号索引的函数指针数组,展开 SYSCALL_DEFINE* 宏如何往这个表注册。',
    ].join('\n'),
  },
  {
    id: 'P2-09',
    file: 'P2-09-参数传递与系统调用返回.md',
    task: [
      '【第 2 篇·第 9 章·参数传递与系统调用返回】',
      '- 核心问题:用户态传给内核的指针怎么安全进内核——copy_from_user/copy_to_user 的边界检查(防用户传非法指针);六寄存器传参约定与栈传参;返回值约定(0 成功、负数表示 -errno);errno 的用户态映射。',
      '- 文件名:P2-09-参数传递与系统调用返回.md',
      '- 二分法归属:进内核',
      '- 关键源码:linux/kernel/sys.c(大量 SYSCALL_DEFINE* 示例,展开宏看参数与返回);linux/lib/usercopy.c 与 arch/x86/lib/copy_user_* 描述(copy_from_user/copy_to_user 实现、access_ok 检查);linux/include/linux/uaccess.h(get_user/put_user/__get_user 宏)。',
      '- 本章典型技巧:copy_from_user 的 page fault fixup 表(用户态指针可能非法,内核解引用触发 page fault,内核用专用的 __ex_table 异常表把 fault 路径跳回 fixup、返回 -EFAULT,不 panic)。',
      '- 反面对比:若内核直接解引用用户传入的非法指针,page fault 在内核态会 panic/oops;fixup 表让"用户传错"降级为 -EFAULT 返回。',
      '- ★对照:否',
      '- 前置衔接:从 P2-08 进了入口、查到 sys_call_table 后,讲"参数怎么进来、返回怎么出去"。',
      '- 特殊要求:展开 SYSCALL_DEFINE* 宏(它是如何定义系统调用、注册到表、包一层参数检查的);讲 fixup 表(__ex_table)的机制;arch/x86 未 clone,copy_user 用描述 + 引 uaccess.h。',
    ].join('\n'),
  },
  {
    id: 'P2-10',
    file: 'P2-10-VDSO-不进内核的系统调用.md',
    task: [
      '【第 2 篇·第 10 章·VDSO:不进内核的"系统调用"】',
      '- 核心问题:gettimeofday/clock_gettime 每秒可能被调百万次,真走系统调用开销巨大;VDSO(Virtual Dynamic Shared Object)把内核映射的共享页 + 用户态函数,让"读时间"完全不进内核:内核把墙上时间写在共享页,用户态直接读。',
      '- 文件名:P2-10-VDSO-不进内核的系统调用.md',
      '- 二分法归属:进内核(VDSO 是"避免进内核"的优化,服务系统调用这一面)',
      '- 关键源码:arch/x86/entry/vdso/(未 sparse clone,描述 VDSO 如何被映射进每个进程、vdso 符号 gettimeofday/clock_gettime);linux/kernel/time/timekeeping.c(内核侧更新共享页的 gtod);linux/kernel/time/vsyscall.c(update_vsyscall 写共享页)。',
      '- 本章典型技巧:VDSO 共享页 + seqlock(内核写时间时 seqlock 偶数→奇数→偶数,用户态读到奇数版本号代表正在写就重试,读到偶数才用;无锁又一致)。',
      '- 反面对比:若每次读时间都进内核,百万级 QPS 直接拖垮 CPU(系统调用开销 vs 一条内存读)。',
      '- ★对照:是(对照 Tokio:Tokio 用 Instant::now() 读粗粒度时间也走类似共享页思路;对照 Go runtime:Go 的 time.Now 在 VDSO 之上再优化)',
      '- 前置衔接:从 P2-08/09 系统调用的开销接,讲"有一种系统调用干脆不进内核"。',
      '- 特殊要求:讲清 seqlock 读者侧(用户态)与写者侧(内核)的配合,这是无锁高并发读时间的关键;arch/x86 未 clone,VDSO 用描述 + 引 timekeeping.c/vsyscall.c。',
    ].join('\n'),
  },
  {
    id: 'P2-11',
    file: 'P2-11-系统调用追踪-seccomp与ftrace.md',
    task: [
      '【第 2 篇·第 11 章·系统调用追踪:seccomp 与 ftrace】',
      '- 核心问题:怎么在系统调用入口前后插钩子——seccomp(用 BPF 程序过滤系统调用,沙箱/容器安全核心,在系统调用真正执行前决定允许/拒绝/trace);ftrace/trace_events(观测系统调用延迟、参数、返回值);/proc/<pid>/syscall 看当前系统调用。',
      '- 文件名:P2-11-系统调用追踪-seccomp与ftrace.md',
      '- 二分法归属:进内核',
      '- 关键源码:linux/kernel/seccomp.c(seccomp_check/filter、__seccomp_filter 在系统调用入口前跑 BPF);linux/kernel/entry/common.c(seccomp 与 syscall_enter 的衔接点);linux/kernel/trace/(ftrace/trace_events 对 syscalls 的埋点 trace_sys_enter/trace_sys_exit);linux/include/uapi/linux/seccomp.h(SECCOMP_RET_* 返回值,未 sparse clone 描述)。',
      '- 本章典型技巧:seccomp BPF 在系统调用入口前过滤(BPF 程序拿到系统调用号与参数,返回 ALLOW/KILL/TRAP/ERRNO/TRACE;在真正执行系统调用之前拦截,开销远小于"执行后再检查")。',
      '- 反面对比:若系统调用执行之后才检查,已经被系统调用执行开销 + 可能已产生副作用;入口前过滤又快又安全。',
      '- ★对照:否',
      '- 前置衔接:从 P2-08/09 系统调用入口与参数接,讲"入口还能插安全/观测钩子"。',
      '- 特殊要求:讲清 seccomp 与 BPF 的关系(seccomp 是 eBPF 的早期用户之一);ftrace 的 trace_sys_enter/trace_sys_exit 埋点位置;为第 3 篇时钟收尾(系统调用篇到此结束)。',
    ].join('\n'),
  },
  {
    id: 'P3-12',
    file: 'P3-12-clocksource-clockevent-硬件时钟抽象.md',
    task: [
      '【第 3 篇·第 12 章·clocksource/clockevent:硬件时钟抽象】',
      '- 核心问题:时钟硬件五花八门(x86 TSC/HPET/PIT/ACPI PM Timer、ARM arch_timer),内核怎么抽象——两套:struct clocksource(只读、单调、提供 read() 返回纳秒 cycle,墙上时间/时间间隔用)、struct clock_event_device(可编程的定时中断设备,触发 tick/hrtimer 用)。',
      '- 文件名:P3-12-clocksource-clockevent-硬件时钟抽象.md',
      '- 二分法归属:支撑',
      '- 关键源码:linux/kernel/time/clocksource.c(clocksource 注册、watchdog 互校、__clocksource_select 选最佳);linux/kernel/time/clockevents.c(clock_event_device 注册、set_next_event 编程);linux/include/linux/clocksource.h(struct clocksource@96、read 函数指针、rating、mask);linux/include/linux/clockchips.h(struct clock_event_device)。',
      '- 本章典型技巧:clocksource 的 read() 抽象 + watchdog(多个时钟源互相校验,TSC 若跑飞会被 watchdog 检出并自动降级到 HPET,反面对比"硬件直接暴露会让驱动各写一份、且无法处理 TSC 不稳定")。',
      '- 反面对比:若每个驱动直接读硬件计数器,会有多份重复代码、无法统一纳秒换算、无法检测某个时钟源漂移。',
      '- ★对照:否',
      '- 前置衔接:从第 2 篇系统调用接,开启第 3 篇时钟(内核主动驱动的心跳)——先讲时钟硬件怎么抽象,才有后面 timekeeping/hrtimer。',
      '- 特殊要求:讲清 clocksource(只读计数)与 clock_event_device(可编程定时中断)的分工——这是理解 hrtimer 编程与 tick 的前提;讲 TSC 不稳定与 watchdog 降级。',
    ].join('\n'),
  },
  {
    id: 'P3-13',
    file: 'P3-13-timekeeping-墙上时间怎么维护.md',
    task: [
      '【第 3 篇·第 13 章·timekeeping:墙上时间怎么维护】',
      '- 核心问题:墙上时间(REAL)/单调时间(MONOTONIC)/boot 时间怎么从 clocksource 累积出来——struct timekeeper(持有 xtime 墙上时间、monotonic、raw、NTP 校正参数 mult/shift);ktime_get/ktime_get_ts/ktime_get_real/ktime_get_boottime 系列 API;NTP 校正(adjtimex 系统调用、ntp.c 微调 mult 拉快/拉慢漂移);时间只向前修正(NTP 负偏移时不让墙上时间倒退)。',
      '- 文件名:P3-13-timekeeping-墙上时间怎么维护.md',
      '- 二分法归属:支撑',
      '- 关键源码:linux/kernel/time/timekeeping.c(ktime_get_* 系列、timekeeping_advance 累积、timekeeping_notify);linux/kernel/time/ntp.c(NTP 校正、ntp_tick_adj);linux/include/linux/timekeeper_internal.h(struct timekeeper@92、struct tk_read_base@34,持有 clocksource 与 cycle_last/mult/shift)。',
      '- 本章典型技巧:timekeeper 的 seqlock 写 + per-CPU 缓存(写墙上时间时 seqlock 奇数版本号,ktime_get 读者读到奇数就重试、读到偶数才用,无锁高并发;配合 VDSO 的 P5-21 seqlock 回扣)。',
      '- 反面对比:若用全局自旋锁保护 timekeeper,多核上 ktime_get(极高频)会排队成性能瓶颈;seqlock 让读路径无锁。',
      '- ★对照:否',
      '- 前置衔接:从 P3-12 clocksource 提供 read() 接,讲"内核怎么把周期性 read 累积成墙上时间"。',
      '- 特殊要求:讲 mult/shift 把 cycle 换算成纳秒的定点运算技巧(为何用 mult+shift 而非浮点);讲 seqlock 读写配合(为 P2-10 VDSO、P5-21 收束回扣);NTP 校正是微调 mult 而非直接改时间。',
    ].join('\n'),
  },
  {
    id: 'P3-14',
    file: 'P3-14-hrtimer-高精度红黑树定时器.md',
    task: [
      '【第 3 篇·第 14 章·hrtimer:高精度红黑树定时器 ⚠️ 核心重头戏】',
      '- 核心问题:hrtimer 取代老的低精度 timer wheel——每个 CPU 一个 hrtimer_cpu_base,内含多个 hrtimer_clock_base(MONOTONIC/REAL/BOOT/TAI),每个 base 一棵红黑树(timerqueue,基于红黑树)按到期时间排序;hrtimer_start 入队、__hrtimer_run_queues 扫到期、hrtimer_interrupt 处理到期;softexpires 区间允许合并唤醒、减少 clockevent 重编程与中断。',
      '- 文件名:P3-14-hrtimer-高精度红黑树定时器.md',
      '- 二分法归属:内核主动(时钟驱动)',
      '- 关键源码:linux/kernel/time/hrtimer.c(__hrtimer_next_event_base@505 求最早到期、__hrtimer_run_queues@1724 扫到期并回调、hrtimer_interrupt@1788 高精度中断入口、hrtimer_run_queues@1901 低精度回退路径、enqueue_hrtimer/remove_hrtimer);linux/include/linux/hrtimer_types.h(struct hrtimer@39、enum hrtimer_restart@13);linux/include/linux/hrtimer.h(hrtimer_cpu_base/hrtimer_clock_base、enum hrtimer_mode@35、hrtimer_start/range)。',
      '- 本章典型技巧:红黑树(timerqueue) + softexpires 区间(O(log n) 找最早到期、最早到期者才会触发 clockevent 重编程;softexpires 给 timer 一个可被提前唤醒的区间,让多个 timer 的"最早可唤醒点"对齐,避免每次加 timer 都重编程、避免 thundering herd 中断风暴)。',
      '- 反面对比:朴素按精确到期唤醒,每个 timer 一个精确中断 → 中断风暴;无红黑树用链表 → 找最早到期 O(n),定时器多了性能崩。',
      '- ★对照:是(对照 Tokio 时间轮 tokio::time::wheel:内核用红黑树求精确 O(log n),Tokio 用层级时间轮求批量 O(1),不同的取舍——精确 vs 批量)',
      '- 前置衔接:从 P3-12/13 时钟硬件与墙上时间接,讲"基于可编程 clock_event_device 的高精度定时器"。',
      '- 特殊要求:⚠️ 用 ASCII 框图画 hrtimer_cpu_base → 4 个 hrtimer_clock_base → 各自 timerqueue 红黑树的嵌套;讲 timerqueue 是红黑树的封装;讲 softexpires(slack)如何减少重编程;务必回扣调度器第 11 本 P1-04 的 hrtick(本书读者该理解 sched 的 hrtick 就是 hrtimer 的薄包装,实现 EEVDF 的精确抢占——读 linux-sched/ 下 hrtick 相关代码);别照搬 timer wheel 老资料。',
    ].join('\n'),
  },
  {
    id: 'P3-15',
    file: 'P3-15-tick与NOHZ-动态时钟省电.md',
    task: [
      '【第 3 篇·第 15 章·tick 与 NOHZ:动态时钟省电】',
      '- 核心问题:周期性 tick(每 HZ 一次,如 100/250/1000)驱动统计/调度/时间累积,但 CPU 进 idle 空跑 tick 浪费电、增加虚拟化噪声;NOHZ(NO_HZ_IDLE)让 CPU 进 idle 时停掉下一个 tick,由最近一个 hrtimer 到期时唤醒;NOHZ_FULL(给 CPU 跑单一 CPU 密集任务的实时/虚拟化场景,几乎完全无 tick)。',
      '- 文件名:P3-15-tick与NOHZ-动态时钟省电.md',
      '- 二分法归属:内核主动',
      '- 关键源码:linux/kernel/time/tick-sched.c(tick_sched_do_timer@206、tick_nohz_restart_sched_tick@1086、tick_nohz_idle_enter@1250、tick_nohz_irq_exit@1287、tick_nohz_stop_sched_tick 计算下一个事件);linux/kernel/time/tick-oneshot.c(单次模式编程);linux/kernel/time/tick-sched.h(struct tick_sched)。',
      '- 本章典型技巧:NOHZ idle 停 tick 的"借下一个 hrtimer"(进 idle 时把周期 tick 取消,改让最近的 hrtimer(或 timer 迁移层)充当唤醒源,CPU 真正进入深睡;醒来后再补 tick 计数)。',
      '- 反面对比:无 NOHZ 的 CPU idle 时还在每秒数百次空转 tick,服务器功耗、笔记本续航、虚拟化噪声都巨大。',
      '- ★对照:是(对照 Tokio:进程空闲时 park 线程,没事干就不空转,同思路)',
      '- 前置衔接:从 P3-14 hrtimer 接,讲"既然有高精度 hrtimer,周期 tick 就可以按需开关"。',
      '- 特殊要求:讲清 NOHZ_IDLE(进 idle 停 tick)与 NOHZ_FULL(跑任务时也几乎无 tick,给 CPU 密集/实时任务)的区别与各自场景;依赖 P3-14 hrtimer 知识;回扣调度器(无 tick 下调度器如何工作)。',
    ].join('\n'),
  },
  {
    id: 'P3-16',
    file: 'P3-16-POSIX-timer-itimer-用户态定时器API.md',
    task: [
      '【第 3 篇·第 16 章·POSIX timer/itimer:用户态定时器 API】',
      '- 核心问题:用户态的 setitimer/timer_create/timer_settime 怎么落地——内核把 POSIX 定时器映射成 hrtimer(墙上/单调时间定时)或 CPU timer(进程 CPU 时间消耗的定时,posix-cpu-timers.c);SIGALRM/SIGPROF 到期投递;与 PELT(调度器负载统计)的关系。',
      '- 文件名:P3-16-POSIX-timer-itimer-用户态定时器API.md',
      '- 二分法归属:内核主动(连接时钟和信号,呼应第 4 篇)',
      '- 关键源码:linux/kernel/time/posix-timers.c(timer_create/timer_settime、struct k_itimer 内嵌 hrtimer);linux/kernel/time/posix-cpu-timers.c(CPU 时间定时器、检查进程 CPU 时间消耗);linux/kernel/time/itimer.c(setitimer 是 hrtimer 的薄包装);linux/include/linux/posix-timers.h。',
      '- 本章典型技巧:POSIX timer 复用 hrtimer(老的 itimer 只是 hrtimer 的一层薄包装,timer_create 也内嵌 hrtimer;统一机制避免定时器碎片化)。',
      '- 反面对比:若每种用户定时器独立实现,itimer/timer_create/CPU timer 各搞一套,机制碎片化、难维护。',
      '- ★对照:否',
      '- 前置衔接:从 P3-14 hrtimer 与 P3-15 NOHZ 接,讲"用户态看到的定时器 API 都建在 hrtimer 之上"。',
      '- 特殊要求:讲清 itimer(简单,SIGALRM)与 POSIX timer(timer_create,可携带 siginfo 的实时信号)的区别;CPU timer(CPU 时间而非墙上时间)的特殊性;本章连接时钟篇与信号篇(定时器到期靠信号通知)。',
    ].join('\n'),
  },
  {
    id: 'P4-17',
    file: 'P4-17-信号投递-send_sig-complete_signal.md',
    task: [
      '【第 4 篇·第 17 章·信号投递:send_sig/complete_signal】',
      '- 核心问题:kill/tgkill/raise/rt_sigqueueinfo 这些入口怎么把信号送到目标进程——do_send_sig_info → __send_signal_locked → complete_signal,挂到目标进程/线程组的 pending 队列(struct sigpending + sigqueue 链表);进程级(整个线程组)vs 线程级(特定 task)信号;信号编号(1-31 普通 / 34-64 实时)与默认动作(忽略/终止/core/stop)。',
      '- 文件名:P4-17-信号投递-send_sig-complete_signal.md',
      '- 二分法归属:内核主动(信号投递)',
      '- 关键源码:linux/kernel/signal.c(do_send_sig_info@1294 入口、__send_signal_locked@1074 选 pending 队列并挂 sigqueue、complete_signal@995 决定唤醒哪个线程、prepare_signal 判断是否可投递);linux/include/linux/sched/signal.h(struct sighand_struct@21 持 sigaction 数组、struct signal_struct@92 线程组共享、struct sigpending、struct sigqueue)。',
      '- 本章典型技巧:sigpending 队列 + 信号合并(普通信号 1-31 同编号在 pending 位图里只挂一次、重复发不排队;实时信号 34-64 用 sigqueue 链表排队不丢、可携带 siginfo)。',
      '- 反面对比:若用单个全局位图表示信号(无 sigqueue 链表),无法表达信号携带的 siginfo 数据、实时信号会丢。',
      '- ★对照:是(对照 Go channel:投递延迟到收方稍后取,发送方与接收方解耦)',
      '- 前置衔接:从第 3 篇时钟(定时器到期投递信号)接,开启第 4 篇信号——先讲信号怎么从发送方挂到目标。',
      '- 特殊要求:用 ASCII 框图画 task_struct → sighand_struct(sigaction[])→ signal_struct(共享 pending)→ sigpending(sigqueue 链表 + 位图)的嵌套;讲清普通信号合并与实时信号排队的区别;complete_signal 如何挑一个合适的线程唤醒。',
    ].join('\n'),
  },
  {
    id: 'P4-18',
    file: 'P4-18-处理入口-返回用户态检查_TIF_SIGPENDING.md',
    task: [
      '【第 4 篇·第 18 章·处理入口:返回用户态检查 _TIF_SIGPENDING】',
      '- 核心问题:信号投递(P4-17)只是"挂账",真正跑 handler 在返回用户态之前——exit_to_user_mode_loop 检查 thread_info->flags 的 _TIF_SIGPENDING/_TIF_NOTIFY_RESUME/_TIF_NEED_RESCHED 等位,有信号则 arch_do_signal_or_restart → get_signal 取一个信号(处理默认动作/忽略/用户 handler)→ handle_signal 跳到用户态 handler。',
      '- 文件名:P4-18-处理入口-返回用户态检查_TIF_SIGPENDING.md',
      '- 二分法归属:内核主动',
      '- 关键源码:linux/kernel/entry/common.c(exit_to_user_mode_loop@90 主循环检查 ti_work 各位、syscall_exit_to_user_mode@215、arch_do_signal_or_restart@83 弱符号);linux/kernel/signal.c(get_signal@2675 取信号并决定动作、do_signal);linux/include/linux/thread_info.h(_TIF_SIGPENDING 等标志位)。',
      '- 本章典型技巧:信号延迟到返回用户态才处理(内核只挂 pending,handler 必须在用户态跑——因为 handler 是用户代码、要访问用户栈与用户数据、且内核不该执行用户代码)。',
      '- 反面对比:若内核态直接跑 handler,会破坏用户上下文、且 handler 本身是不可信的用户代码,内核不能执行。',
      '- ★对照:是(对照 Go:panic 同步进 runtime 杀死 goroutine,channel 收方主动取;信号是"内核记账、用户态跑 handler"的折中)',
      '- 前置衔接:从 P4-17 信号挂上 pending 接,讲"挂上的信号什么时候、在哪里真正处理"。',
      '- 特殊要求:讲清为何选在"返回用户态"这个点检查(线程边界、安全的上下文切换点);exit_to_user_mode_loop 同时检查 _TIF_NEED_RESCHED(调度)、_TIF_SIGPENDING(信号)、_TIF_NOTIFY_RESUME(resume 通知),回扣中断/调度;为何不能在任意点处理信号。',
    ].join('\n'),
  },
  {
    id: 'P4-19',
    file: 'P4-19-sigaction与handler栈切换-rt_sigreturn.md',
    task: [
      '【第 4 篇·第 19 章·sigaction 与 handler 栈切换:rt_sigreturn】',
      '- 核心问题:sigaction 注册 handler 后,信号到达时内核怎么让 handler 跑起来又能回到原现场——内核在用户栈上构建 ucontext + siginfo 帧(让 handler 像普通函数被调用、又能恢复被打断的寄存器),把返回地址改成 rt_sigreturn 系统调用入口;handler 跑完"返回"时自动触发 rt_sigreturn 回内核、恢复被打断的现场;可选 SA_ONSTACK + sigaltstack(自定义信号栈,主栈溢出时还能跑 handler)。',
      '- 文件名:P4-19-sigaction与handler栈切换-rt_sigreturn.md',
      '- 二分法归属:内核主动',
      '- 关键源码:arch/x86/kernel/signal.c(__setup_rt_frame,未 sparse clone,描述在用户栈构建 rt_sigframe 的布局 + 改返回地址为 rt_sigreturn);linux/kernel/signal.c(do_sigaction 注册、handle_signal 调 setup_frame);linux/include/uapi/linux/signal.h(SA_ONSTACK 等标志,未 clone 描述)。',
      '- 本章典型技巧:用户栈上构建 sigframe + rt_sigreturn 回跳(让 handler 像普通函数调用,但"返回地址"被劫持成恢复原上下文的系统调用;巧妙复用函数调用返回机制)。',
      '- 反面对比:若内核保存完整用户上下文并维护复杂状态机来恢复,实现复杂、易错;复用用户栈 + 劫持返回地址是最小开销。',
      '- ★对照:否',
      '- 前置衔接:从 P4-18 决定跑 handler 接,讲"handler 具体怎么被调用、怎么返回"。',
      '- 特殊要求:arch/x86 未 clone,__setup_rt_frame 用描述 + 引 signal.c;讲清 sigframe 在用户栈的布局(ucontext + siginfo + trampoline 返回地址);讲 SA_ONSTACK/sigaltstack 为何需要(栈溢出时的 SIGSEGV handler 还要有栈可跑);rt_sigreturn 的"劫持返回"机制是本章精华。',
    ].join('\n'),
  },
  {
    id: 'P4-20',
    file: 'P4-20-信号与异常的关系.md',
    task: [
      '【第 4 篇·第 20 章·信号与异常的关系】',
      '- 核心问题:CPU 异常(缺页 → SIGSEGV/可修复则不投递、除零 → SIGFPE、非法指令 → SIGILL、栈越界 → SIGSEGV)本质上也是"内核向进程发信号";异常处理路径:CPU 触发 → 内核异常入口 → 判断是否可修复(如缺页按需调页)→ 不可修复则 force_sig 投递;与硬件中断的区别(异常同步于当前指令、中断异步外部);与信号的统一(都是"事件通知进程")。',
      '- 文件名:P4-20-信号与异常的关系.md',
      '- 二分法归属:进内核(异常) + 内核主动(信号)的桥梁',
      '- 关键源码:linux/kernel/signal.c(force_sig_info/force_sig、force_sig_fault 等异常专用投递);arch/x86 异常入口描述(缺页 do_page_fault、除零 divide_error 等,未 sparse clone 描述 + 引通用);linux/arch/x86/mm/fault.c 描述(do_user_addr_fault 判可修复 vs 投 SIGSEGV)。',
      '- 本章典型技巧:异常路径与信号投递的统一(异常不可恢复时统一走 force_sig,让用户态用同一套信号 handler 处理硬件错误;可修复的缺页则不投信号、悄悄按需调页)。',
      '- 反面对比:若异常单独一套机制(不通过信号),用户接口碎片化,用户态要分别处理"段错误"和"kill 发的信号"。',
      '- ★对照:否',
      '- 前置衔接:从 P4-17~19 信号的完整投递链接,讲"原来硬件异常也是通过这套信号机制通知用户的"。',
      '- 特殊要求:⚠️ 本章是桥梁章,要点明信号与异常(中断的一种)的统一——回扣全书"事件跨越边界"主线:异常是同步进内核、不可恢复时用内核主动的信号通道通知用户;讲清缺页这类"可修复异常"为何不投信号;为 P5-21 收束铺垫。',
    ].join('\n'),
  },
]

// ===================== 收尾波(正文全部完成后) =====================
const P5_TASK = [
  '【第 5 篇·第 21 章·四个机制的哲学 + ★对照 Tokio/Go/io_uring 总表(全书收束)】',
  '- 文件名:P5-21-四个机制的哲学-对照Tokio-Go-io_uring总表.md',
  '- 核心问题:全书收束——把中断/系统调用/时钟/信号四个机制的共通哲学抽出来:① 延迟处理(softirq 延迟到 IRQ 退出、信号延迟到返回用户态,都在"安全点"处理);② per-CPU 无锁化(softirq pending 位图、hrtimer cpu_base、preempt_count,避免多核锁);③ 上半部/下半部切分(中断 hardirq/softirq、hrtimer 软中断模式);④ seqlock 无锁读(timekeeper/VDSO/hrtimer 都用奇偶版本号让读者无锁);⑤ 受控通道(SYSCALL 是用户合法进内核、信号是内核通知用户,都是"边界上的门")。然后给一张 Linux 内核机制 vs Tokio vs Go runtime vs io_uring 对照总表。',
  '- 二分法归属:收束',
  '- 关键源码:全书回顾(引各章已贴的关键源码点,不新引大段代码)。',
  '- 本章典型技巧:内核事件模型 vs 用户态运行时事件模型(同样是"事件驱动",内核用硬件中断 + per-CPU + 延迟处理,用户态运行时用 epoll + 时间轮 + task 调度,取舍不同)。',
  '- ★对照:是(全对照:中断↔io_uring CQE、上下半部↔Tokio mio+task、hrtimer↔Tokio 时间轮、NOHZ↔Tokio park、信号↔Go channel/panic、SYSCALL↔Go systrap)',
  '- 前置衔接:本书前 20 章已全部写完(在 linux-内核机制设计与实现/ 目录,动手前用 Read 通读各章小结以回顾全书),本章把它们收束成一张图、一张表。',
  '- 特殊要求:本章是收束章,① 必须用一张大表(Linux 内核机制 vs Tokio vs Go runtime vs io_uring,按"事件源/通知方式/延迟处理/无锁手段/取舍"维度)把全书的★对照钉成"事件驱动全栈";② 用 mermaid 画一张"一次事件从硬件到用户 handler 的全旅程"总图(把中断→softirq→系统调用入口→时钟→信号串起来);③ 五个哲学点每点回扣具体章节;④ 附录 A/B 将单独由其他 agent 写,本章末尾点一句"全景脉络见附录 A、源码路线见附录 B"。',
  '- 注意:作为收束章,允许篇幅略长(10000~13000 字),把全书哲学讲透、对照表给全。',
].join('\n')

const APP_A_TASK = [
  '【附录 A·全景脉络(时序图与状态机合集,非标准四段式章节,但保持真实源码/标点/风格)】',
  '- 文件名:附录A-全景脉络.md',
  '- 核心问题:把全书四个机制的端到端旅程画成图,作为"读完全书后的回顾索引"。',
  '- 内容(全部用 mermaid 画,标注关键源码函数名 + 行号):',
  '  A.1 一次网卡中断从硬件触发到 NAPI 软中断的端到端时序图(sequenceDiagram:硬件中断 → IDT → handle_arch_irq → __handle_irq_event_percpu → 驱动 ISR 上半部 → raise_softirq(NET_RX) → irq_exit → invoke_softirq → __do_softirq → handle_softirqs → net_rx_action → NAPI 轮询)。',
  '  A.2 一次系统调用从用户态到内核返回的状态机/时序图(SYSCALL → do_syscall_64 → syscall_enter_from_user_mode_prepare → sys_call_table[nr] → copy_from_user → 返回 → syscall_exit_to_user_mode → exit_to_user_mode_loop 检查 ti_work)。',
  '  A.3 hrtimer/NOHZ 时钟状态机(stateDiagram:高精度模式启停、进 idle 停 tick、由最近 hrtimer 唤醒、tick_nohz_restart_sched_tick 补 tick)。',
  '  A.4 信号投递到 handler 的完整流程图(flowchart:kill → do_send_sig_info → __send_signal_locked → complete_signal 挂 pending → 目标线程返回用户态 → exit_to_user_mode_loop 查 _TIF_SIGPENDING → get_signal → handle_signal → __setup_rt_frame → 用户 handler → rt_sigreturn 恢复)。',
  '- 关键源码:全书各章关键函数(行号以你 Grep 核实为准,参考主控给的锚点)。',
  '- ★对照:否(附录以图为主)',
  '- 特殊要求:四张图必须覆盖全书四个机制,每张图标注关键函数名与所在文件;图前用一两句话点出"这张图串起哪几章";附录开头一句话说明"这是全书的脉络索引,读完正文后回来对照"。',
].join('\n')

const APP_B_TASK = [
  '【附录 B·源码阅读路线与延伸(非标准四段式章节,保持真实源码路径/标点/风格)】',
  '- 文件名:附录B-源码阅读路线与延伸.md',
  '- 核心问题:给读者一份"读内核这四个子系统源码的地图 + 观测手段 + 调参 + 跨 OS 对照"的索引。',
  '- 内容:',
  '  B.1 源码阅读地图:按本书分篇给阅读顺序——kernel/irq/(handle.c/manage.c/chip.c/irqdomain.c)→ kernel/softirq.c → kernel/workqueue.c → kernel/entry/common.c + arch/x86/entry/ → kernel/time/(clocksource.c/timekeeping.c/hrtimer.c/tick-sched.c)→ kernel/signal.c;每处给"先读哪个函数、再读哪个"的路线。',
  '  B.2 观测手段:/proc/interrupts(每 IRQ 计数)、/proc/softirqs(每 CPU 各类软中断计数)、/proc/<pid>/status 的 SigQ/SigPnd/ShdPnd(信号 pending)、/proc/<pid>/timers(进程定时器)、/proc/timer_list(hrtimer/clocksource 现状)、/proc/sys/kernel/{hz,hostname};perf(系统调用延迟、软中断开销)、ftrace(trace_sys_enter/exit、irq 跟踪)、strace(系统调用)、bpftrace(任意探点)各给一个例子。',
  '  B.3 与其他 OS 对照:BSD 的 intr(中断)、Windows 的 IRQL(中断请求级,类似 preempt_count 的分层)、其他 OS 的系统调用/时钟/信号机制简比。',
  '  B.4 调参:CONFIG_HZ(tick 频率)、isolcpus(隔离 CPU)、nohz_full(全无 tick)、irqaffinity(中断亲和)、RT preempt(PREEMPT_RT 实时补丁)各讲何时用、改了什么。',
  '- ★对照:否',
  '- 特殊要求:附录是"延伸索引",不展开讲实现(那是正文的事),只给路线 + 命令例子 + 指针;每条观测命令给一句"看什么";保持全书标点与风格。',
].join('\n')

// ===================== 返回报告 schema =====================
const REPORT = {
  type: 'object',
  additionalProperties: false,
  properties: {
    chapter: { type: 'string', description: '章节标识,如 P1-06 / P5-21 / 附录A' },
    path: { type: 'string', description: '写入的文件相对路径,如 linux-内核机制设计与实现/P1-06-...md' },
    wordCount: { type: 'integer', description: '正文字数(粗估,中文字符数)' },
    keySourceFiles: {
      type: 'array',
      items: { type: 'string' },
      description: '本章引用的关键源码文件(相对路径),如 linux/kernel/softirq.c',
    },
    sourceFactsCorrected: {
      type: 'string',
      description: '写作中 Grep/Read 核实后,发现总纲/提示词与 6.9 真实源码不符、自己主动修正的印象(行号/符号名/字段/参数)。务必如实填写,这是主控核验的重要依据。若无修正填"无"。',
    },
    hasTechSection: { type: 'boolean', description: '是否包含"技巧精解"小节' },
    hasContrast: { type: 'boolean', description: '是否包含"★对照"小栏(非标★章填 false)' },
    selfCheckNotes: { type: 'string', description: '自检清单结果摘要 + 任何需主控注意的问题(如某符号 6.9 改名、某行号只能标文件)' },
  },
  required: ['chapter', 'path', 'wordCount', 'keySourceFiles', 'sourceFactsCorrected', 'hasTechSection', 'hasContrast'],
}

// ===================== 执行 =====================
phase('正文')
log('开始写正文 19 章(P1-02~P4-20),篇间并行,每个 agent 独立 Grep 核实源码')
const bodyResults = await parallel(
  CHAPTERS.map((ch) => () =>
    agent(COMMON + ch.task, { label: ch.id, phase: '正文', schema: REPORT }),
  ),
)

phase('收尾')
log('正文完成,开始收尾波:P5-21 对照总表 + 附录A 全景脉络 + 附录B 源码路线(回顾全书)')
const tailResults = await parallel([
  () => agent(COMMON + P5_TASK, { label: 'P5-21', phase: '收尾', schema: REPORT }),
  () => agent(COMMON + APP_A_TASK, { label: '附录A', phase: '收尾', schema: REPORT }),
  () => agent(COMMON + APP_B_TASK, { label: '附录B', phase: '收尾', schema: REPORT }),
])

return {
  body: bodyResults.filter(Boolean),
  tail: tailResults.filter(Boolean),
  bodyFailed: CHAPTERS.map((c) => c.id).filter((_, i) => !bodyResults[i]),
  summary: `正文 ${bodyResults.filter(Boolean).length}/${CHAPTERS.length} 完成;收尾 ${tailResults.filter(Boolean).length}/3 完成`,
}
