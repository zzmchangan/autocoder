# 新会话接力 prompt（复制下方「开始」到「结束」之间全部内容，粘贴进新会话）

<!-- 开始 -->

你将接手并完成《深入浅出系列》的《Tokio 设计与实现深入浅出：从一根线程到百万并发》一书。

**工作目录**：`c:\Users\86133\Desktop\深入浅出系列`
**当前进度**：总纲、写作提示词、P0-01 样板均已写好；`tokio`(1.52.3 @ 7892f60) + `mio`(1.2.1 @ 80c693c) 源码已 clone 到系列根目录(`../tokio/`、`../mio/`)。
**你的目标**：用多 agent 并行，写完剩余 20 章(P1-02 ~ P7-21)，范围是全书第 0~7 篇全部写完。

## 第一步：必读（重建上下文，按顺序）
1. `tokio设计与实现/全书规划-总纲.md` —— 主线(调度执行 vs 事件唤醒)、二分法、服务员比喻对照表、分篇分章、源码策略。
2. `tokio设计与实现/_章节写作提示词.md` —— **这是你的执行手册**：写作铁律、四段式+技巧精解、源码规范、配图、标点、**附 21 章清单与并行分组**。
3. `tokio设计与实现/P0-01-第一性原理-为什么需要异步运行时.md` —— **风格样板**，所有 agent 一律对齐它的文风/比喻/结构/源码引用。
4. 已加载的 memory 方法论：`source-code-series-method`(动机+技巧双线)、`teach-math-geometry-first`(标点约定：**全角句号**)。

## 铁律（每一章都必须遵守）
- **动机(why) + 技巧(how) 双线**：每章单开"技巧精解"小节，挑最硬核 1~2 个技巧、配真实源码 + 反面对比("不这么写会怎样")拆透。**讲不清技巧 = 没讲 tokio。**
- **源码严禁凭记忆**：基于 `../tokio/`(1.52.3) + `../mio/`(1.2.1)；每处引用先 Grep/Read 核实行号，不确定就**只标文件不标行号**。简化代码必须标注"(简化示意)"。
- **标点**：半角 `,` `:` `()` `?` + 全角 `、` `——` `""` `。`（**句号全角**），中英文之间留空格。
- **配图**：时序/状态/流程用 `mermaid`；内存/数据结构布局用 ASCII 框图（对标数据库系列画 B+tree 页面，**不是**被嫌弃的几何 ASCII）。
- **比喻**：餐厅服务员（总纲对照表内），不得另起炉灶。
- **命名**：`tokio设计与实现/P{篇}-{章}-标题.md`；每章约 8000~12000 字。

## 执行方式：用 Agent 工具多 agent 并行（按提示词附录的分组）
- **第一波（顺序，1 个 agent）**：第 1 篇 P1-02 → P1-03 → P1-04 → P1-05（Future/async-await/Waker/Task 有依赖，必须顺序写）。
- **第二波（六篇并行，6 个 agent 同时启动）**：P2 / P3 / P4 / P5 / P6 / P7 各开一个 agent；篇内按章序推进。
- 每个 agent 的任务模板：读"提示词 + 总纲 + 本篇相邻章 + P0-01 样板" → Grep 核源码 → 写指定章节 → 过自检清单 → 产出 `P{篇}-{章}-标题.md`。
- **并行要点**：同一批要并行的 agent，放在**一条消息里多个 Agent 调用**，才会并发跑。

## 自主推进授权
你被充分授权**自主推进**：读文件 → 启动 agent → 核验产出(抽查源码行号是否真实、技巧是否讲透、标点是否全角句号) → 继续下一波，直到 20 章全部完成。每完成一波向我简短汇报(哪些章完成、各章字数、引用的关键源码文件)。只在遇到"风格/范围/深度"这类需要拍板的事时才问我，写作细节自己定。

**现在开始**：先读上面三个文件，然后启动第一波 agent(第 1 篇)。

<!-- 结束 -->
