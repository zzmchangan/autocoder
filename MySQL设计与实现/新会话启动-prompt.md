# 新会话接力 prompt(复制下方「开始」到「结束」之间全部内容,粘贴进新会话)

<!-- 开始 -->

你将接手并完成《深入浅出系列》的《MySQL·InnoDB 设计与实现深入浅出:一次写怎么做到既不丢又高并发》一书。

**工作目录**:`c:\Users\86133\Desktop\深入浅出系列`
**当前进度**:总纲、写作提示词、目录与导读、P0-01 样板均已写好;`mysql-server`(`mysql/mysql-server` 的 C/C++ 实现)源码已 clone 到系列根目录(`../mysql-server/`,master @ `845d525`,版本 9.7.0 LTS;InnoDB 在 `storage/innobase/`)。
**你的目标**:用多 agent 并行,写完 23 章(P0-01 ~ P7-23)+ 附录 AB,范围是全书第 0~7 篇全部写完。

## 第一步:必读(重建上下文,按顺序)
1. `MySQL设计与实现/全书规划-总纲.md` —— 主线、**存储与索引 vs 事务与并发二分法**、四重承接、分篇分章(每章核心问题/源码/技巧)、架构演进交代(第五节)、写作约定、**LuaJIT 级深度标杆(第二节末)**。
2. `MySQL设计与实现/_章节写作提示词.md` —— **这是你的执行手册**:写作铁律、四段式 + 技巧精解、源码规范、配图、标点、**承接前作(聚簇索引→《PG》、B+树→《LevelDB》、buffer pool→《Linux mm》、MVCC→《TiKV》、SQL旅程→《PG》)、架构演进坑(redo 8.0.30 重构/新 DD/instant DDL,老资料大片过时)、附 23 章清单与并行分组**。
3. `MySQL设计与实现/目录与导读.md` —— 每章一句话钩子 + 技巧标签 + 二分法归属,选章 / 排查速查。
4. `MySQL设计与实现/P0-01-第一性原理-为什么需要InnoDB.md` —— **风格样板**,所有 agent 一律对齐它的文风 / 结构 / 源码引用。
5. 已加载的 memory 方法论:`source-code-series-method`(动机 + 技巧双线)、`source-code-book-multi-agent`(多 agent 编排 + 核验三板斧)。

## 铁律(每一章都必须遵守)
- **动机(why) + 技巧(how) 双线**:每章单开"技巧精解"小节,挑最硬核 1~2 个技巧、配真实源码 + 反面对比拆透。**讲不清聚簇索引/redo/2PC/间隙锁 = 没讲 InnoDB。**
- **篇幅 = LuaJIT 级**:每章约 **12000~18000 字**(对标《LuaJIT》第一性原理版,与《gRPC》《TiKV》《Envoy》《Dubbo》一致),三层 why + 反例 + 对比 + 图 + 选型历史,不吝啬篇幅、把问题讲透。
- **源码严禁凭记忆**:基于 `../mysql-server/storage/innobase/`(@ `845d525`,9.x dev);每处引用先 Grep/Read 核实行号,不确定就**只标文件不标行号**。简化代码必须标注"(简化示意)"。
- **承接前作**:聚簇索引对照《PG》(heap vs 索引组织表)、B+树对照《LevelDB》LSM、buffer pool 对照《Linux mm》page cache、MVCC/事务对照《TiKV》、SQL 旅程对照《PG》,一句话带过 + 指路,不重复。
- **架构演进**:以新版(master 9.x)为准,分清 redo 8.0.30 重构(log0write/log0buf/log0files_*)vs 老 log0log.cc 单文件、8.0 新数据字典(替 .frm)、instant DDL、clone、9.x 向量/JS;讲老版本的博客大片过时,不能当唯一依据。
- **标点**:半角 `,` `:` `()` `?` + 全角 `、` `——` `""` `。`(句号全角),中英文之间留空格。
- **配图**:时序(UPDATE 旅程)/状态(事务/crash recovery)/流程(B+树分裂/MVCC 可见性/2PC)用 `mermaid`;内存/数据结构布局(B+树页 16KB 结构/聚簇索引页内容/undo 版本链/buffer pool 三链表)用 ASCII 框图。
- **比喻**:直球为主、比喻点睛。"图书馆"比喻只在 P0-01 点睛,其他章不得沿用做主线。
- **命名**:`MySQL设计与实现/P{篇}-{章}-标题.md`。

## 执行方式:用 Agent 工具多 agent 并行(按提示词附录的分组)
- **第一波(顺序,1 个 agent)**:第 1 篇 P1-02 → P1-03 → P1-04(表就是 B+树)+ 第 3 篇 P3-08 → P3-09 → P3-10 → P3-11 → P3-12(WAL/redo/undo/2PC/crash,全书心脏)。这两篇是地基,顺序写。
- **第二波(四篇并行,4 个 agent 同时启动)**:第 2 篇(P2-05~07)/ 第 4 篇(P4-13~15)/ 第 5 篇(P5-16~19)/ 第 6 篇(P6-20~22)各开一个 agent;篇内按章序推进。
- **第三波(收尾)**:P7-23 + 附录 A + 附录 B 最后写。
- 每个 agent 的任务模板:读"提示词 + 总纲 + 本篇相邻章 + P0-01 样板" → Grep/Read 核源码 → 写指定章节 → 过自检清单 → 产出 `P{篇}-{章}-标题.md`。
- **并行要点**:同一批要并行的 agent,放在**一条消息里多个 Agent 调用**,才会并发跑。

## 核验三板斧(主控每波收稿时做)
1. **Grep 扫结构**:每章是否有章首核心问题 / 正文四段式 / 技巧精解 / 章末五问。
2. **抽源码行号**:每章抽查 2~3 处源码引用,Read `../mysql-server/storage/innobase/` 对应文件,看函数名 / 行号逐字吻合(凭记忆编行号是最常见硬伤)。
3. **看 agent 是否主动修正印象**:好的 agent 会 Grep 后发现"咦,redo 不在 log0log.cc 单文件了,是 log0write/log0buf 拆分的""间隙锁结构变了",在稿子里写出来——这是质量信号。

## 自主推进授权
你被充分授权**自主推进**:读文件 → 启动 agent → 核验产出(抽查源码行号是否真实、技巧是否讲透、承接是否做到、架构演进是否分清新版 vs 老版、标点是否合规) → 继续下一波,直到 23 章 + 附录全部完成。每完成一波向我简短汇报(哪些章完成、各章字数、引用的关键源码模块)。只在遇到"风格 / 范围 / 深度 / 承接取舍"这类需要拍板的事时才问我,写作细节自己定。

**现在开始**:先读上面四个文件,然后启动第一波 agent(第 1 篇 B+树 + 第 3 篇 WAL 地基)。

<!-- 结束 -->
